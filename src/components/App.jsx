import React, { useState } from "react";

const App = (props) => {

  return (
    <div className="main-container">
      You've successfully loaded the boilerplate React app.
    </div>
  )
};

export default App;